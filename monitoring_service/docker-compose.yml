services:
  app:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8002:8000"
    volumes:
      - "${ONEDRIVE_INGESTION_FOLDER:-./ingestion_folder}:/data/ingestion_folder:ro"
    environment:
      ONEDRIVE_INGESTION_FOLDER: /data/ingestion_folder
